NEXT_PUBLIC_SANITY_PROJECT_ID=f9ampmu2
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SITE_URL=https://helo-sable-five.vercel.app

# Viewer-only token used for authenticated draft reads / live preview.
SANITY_API_READ_TOKEN=

# Server-only token with write permission used by the Visual Builder to save drafts and upload images.
SANITY_API_WRITE_TOKEN=

# Configure tokens only in Vercel Environment Variables. Never commit secret values.
